{% extends 'base.html.twig' %}

{% block title %}
	edition
{% endblock %}


{% block body %}
	{{ form_start(form)}}
	<div class="row">
		<div class="col-6">
      {% if aliment.image %}
      <img src="{{asset('images/legumes/' ~ aliment.image)}}" alt="">
      {% endif %}
      <div> {{ form_row(form.image)}}</div>
      <div> {{ form_row(form.nom)}}</div>
    </div>
	<div class="col-6">{{ form_widget(form) }}</div>
	</div>
	<input type="submit" class="btn btn-primary" value="Enregistrer">
	{{ form_end(form) }}
{% endblock %}
